numpy>=1.26,<3
matplotlib>=3.8.0
openenv-core>=0.2.3
pydantic>=2.8.2
streamlit>=1.38.0
openai>=1.0.0
uvicorn>=0.29.0

# ── Local LLM inference (loading VeritaRL TinyLlama checkpoint) ──
# CPU wheels:
#   pip install --upgrade torch --index-url https://download.pytorch.org/whl/cpu
# NVIDIA GPU: pick the matching CUDA index from https://pytorch.org
torch>=2.2
transformers>=4.44
accelerate>=0.33
safetensors>=0.4
huggingface_hub>=0.24

# ── Reproducing training (GPU only, Colab-friendly) ──
# `unsloth` and `bitsandbytes` require CUDA; they will NOT install on
# Windows/CPU. Run training from the Colab notebook instead:
#   https://colab.research.google.com/drive/1n3vF5YYhbj7Ma3plJtYLlVOXDe6IWdQs?usp=sharing
# trl>=0.18
# peft>=0.11
# datasets>=2.19
# bitsandbytes>=0.43  # GPU only
# unsloth @ git+https://github.com/unslothai/unsloth.git

# ── Optional: richer NPC dialogue via a local Ollama server ──
#   1) install Ollama from https://ollama.com/download
#   2) `ollama pull llama3 && ollama serve`
ollama>=0.3.0
