numpy>=1.26,<3
matplotlib>=3.8.0
openenv-core>=0.2.3
pydantic>=2.8.2
streamlit>=1.38.0
openai>=1.0.0          
uvicorn>=0.29.0       
# -- Optional: local LLM inference (loading the GRPO checkpoint) --
# (set RUMOUR_DEMO=1 to skip loading weights entirely).
#   pip install --upgrade torch --index-url https://download.pytorch.org/whl/cpu
# For NVIDIA GPUs use the appropriate CUDA index from https://pytorch.org.
torch>=2.2
transformers>=4.44
accelerate>=0.33
safetensors>=0.4

# Optional: richer NPC dialogue (needs a running Ollama server + `ollama pull llama3`)
ollama>=0.3.0
